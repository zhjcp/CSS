package pojo;

public class Administrator_s extends User{
        public Administrator_s() {
        }

        public Administrator_s(String id, String name, String pwd, String sex, String phone, String email) {
                super(id, name, pwd, sex, phone, email);
        }
}
