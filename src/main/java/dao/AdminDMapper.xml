<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.AdminDMapper">
    <select id="selectById" parameterType="string" resultType="pojo.user.AdminD">
            select * from css.admin_d where id=#{id}
        </select>
</mapper>
